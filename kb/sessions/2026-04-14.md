# Session 2026-04-14

## Studied
- `packages/agent/README.md` in detail, especially event flow, turn semantics, tool execution, queueing, state, and proxy usage
- Key supporting code in `packages/agent/src/agent.ts`, `packages/agent/src/agent-loop.ts`, and `packages/agent/src/types.ts`
- Selected `packages/coding-agent` entrypoints relevant to the handoff into the generic agent layer

## Key Insights
- In the generic agent runtime, a turn is one assistant response plus any tool calls/results caused by that response
- Thinking/reasoning blocks are still part of the same assistant message and therefore the same turn
- `steer` injects after the current turn and before the next LLM call; `followUp` waits until the agent would otherwise stop
- Tool execution mode is agent-wide: `parallel` by default, `sequential` as a safer fallback when ordering matters
- "Preflight" means tool lookup, argument normalization/validation, and `beforeToolCall`; only preflight-successful calls actually execute
- Pi coding-agent keeps agent-level parallelism but adds finer-grained tool-level serialization for same-file mutations
- `initialState.messages` seeds the runtime transcript; `agent.state.messages` is the live mutable transcript

## Files Traced
- `packages/agent/README.md`
- `packages/agent/src/agent.ts`
- `packages/agent/src/agent-loop.ts`
- `packages/agent/src/types.ts`
- `packages/agent/src/proxy.ts`
- `packages/coding-agent/src/core/agent-session.ts`
- `packages/coding-agent/src/core/sdk.ts`
- `packages/coding-agent/src/core/tools/file-mutation-queue.ts`

## Questions Answered
- What a "turn" means in Pi's agent runtime
- Whether thinking/reasoning creates additional turns
- How parallel vs sequential tool execution works
- What "preflight" and "allowed tools" mean in tool execution docs
- Who decides tool scheduling mode in Pi coding-agent
- Why same-file mutation queues are compatible with global parallel mode
- How to think about `initialState.messages` vs `agent.state.messages`
- How steer/follow-up behave in the generic agent layer and in Pi
- What `streamProxy()` is for in browser + backend architectures
- How to send steer/follow-up messages in raw `Agent`, SDK, RPC, and TUI usage

## Next Exact Step
- Trace `packages/coding-agent/src/core/agent-session.ts::prompt()` into `packages/agent/src/agent.ts::{prompt, runPromptMessages, createLoopConfig}` and then `packages/agent/src/agent-loop.ts::{runAgentLoop, runLoop, streamAssistantResponse}`